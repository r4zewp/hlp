name: TZ3

on: 
  push:
    branches: [master]
  commit:
    branches: [master]

steps:
    - uses: actions/checkout@master
    - uses: actions/setup-python@v2
      with:
        python-version: '3.9'
    - run: pip install -r requirements.txt
    - run: pytest